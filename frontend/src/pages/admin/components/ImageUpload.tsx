/**
 * Spec alias — ImageUpload is MediaUpload with the same upload pipeline.
 * Prefer importing ImageUpload in CMS forms for Module 11 alignment.
 */
export { MediaUpload as ImageUpload, type MediaUploadProps as ImageUploadProps } from './MediaUpload';
